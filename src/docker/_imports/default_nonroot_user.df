FROM main

 
# Create a custom user with UID 1234 and GID 1234
RUN groupadd -g 1234 hugo && \
    useradd -m -u 1234 -g hugo hugo \
    && chown -R hugo /src \
    && chown -R hugo /target

USER hugo
