FROM base--image--alpine AS fetcher

ARG TARGETPLATFORM

RUN apk -U upgrade && apk --no-cache add wget curl

ADD _script/pandoc.sh pandoc.sh
RUN sh pandoc.sh

ADD pandoc /files


FROM scratch

COPY --from=fetcher /files /
